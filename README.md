Concurrently pings a list of URLs loaded from file and outputs which connections succeeded and failed.

Allows user to input the maximum number of threads they want to dedicate towards the process.


##Requirements
cURL C++ library
